import { logger } from "../utils/logger.js";

/** Placeholder: wire Notion, Gmail, Calendar, GitHub, then compose with the LLM. */
export async function runDailyBrief(): Promise<void> {
  logger.info("daily brief job ran (Notion/Gmail/Calendar/GitHub integration pending)");
}
